# DeviceForge 路线图 (Roadmap)

> 工业级系统部署调试工具  
> 最后更新：2026-09-03

**当前版本**：v2.8.0 · **平台**：Windows（x64）· **许可**：MIT

---

## 已完成 (v2.0 → v2.1.0 → v2.2.0 → v2.3.0 → v2.5 → v2.6 → v2.7 → v2.8)

| 模块 | 说明 | 版本 |
|------|------|------|
| **文件部署** | FTP/FTPS 并行批量部署，并发度可配 1-8（默认 1），每设备实时进度、CSV/HTML 报告、失败设备一键重试；支持目录递归、选择性部署、远程重命名/新建目录 | v2.0 / v2.5 / v2.8 |
| **SFTP 文件管理** | SshAdapter SFTP 子系统（列目录/上传/下载/删除/重命名/新建目录），双栏 FTP/SFTP 协议切换；通过 IDeployable 复用并行部署、报告和失败重试 | v2.5 / v2.6 / v2.8 |
| **SylixOS FTP 适配** | EPSV 禁用（服务器不支持）、MULTICWD 逐级 CWD（cd() 单级路径限制）、QUOTE 根 URL、递归删除 DELE→RMD | v2.5 |
| **批量命令** | Telnet + SSH 双协议批量命令执行，SSH 密码认证 + TOFU 主机密钥 | v2.0 |
| **WebSocket 通信** | Server/Client 双模式，默认 127.0.0.1 绑定 + 可选 Token 认证 | v2.0 |
| **Modbus 集群测试** | QModbusTcpClient 批量读写寄存器，QTimer 自动刷新 | v2.0 |
| **网络调试中继** | TCP/UDP/组播透明中继代理，双向流量捕获 + Hex 实时视图；组播支持 M→U/M→M 实时转发 | v2.1.0 / v2.8 |
| **网络调试录制回放** | `.nrec` 自定义二进制格式录制 + 按原始时序回放，组播零影响加入抄收 | v2.1.0 |
| **OPC UA 客户端** | open62541 v1.5.5，读/写/订阅/浏览，None 安全策略 + 匿名认证；地址空间浏览 5 列 UI + 类型友好名 + 节点类色块 + × 删除按钮；针对非规范服务端的 ActivateSession policyId 深拷贝补丁；订阅卡死修复（请求超时 + 后台线程 + 移除冗余轮询） | v2.2.0 / v2.3.0 |
| **在线更新 (OTA)** | GitHub Releases API 查询 + zip 下载 + 校验 + 解压 + Updater.exe 独立替换进程 | v2.2.0 |
| **远端预览面板** | 🗑 已移除（v2.4 FTP 双栏替代，原 v2.2.0 实现） | v2.2.0 |
| **构建系统** | CMake 标准构建（qt_standard_project_setup），Debug/Release/RelWithDebInfo，/FS PDB 共享 | v2.2.0 |
| **UI 主题** | 「琴色是动词」工业深色主题，QSS 体系，全控件统一 | v2.0 |
| **本地配置持久化（ConfigStore）** | SQLite 单表 + Windows DPAPI 密码加密；设备/OPC UA endpoint/Modbus 从站/NetRelay/WebSocket/Telnet 偏好 + FTP 凭证全部自动恢复；统一设置面板（Ctrl+,）查看/导入/导出 | v2.3.0 |

---

## 短期规划（v2.8 已交付 → 近期候选）

**v2.8（2026-08-22）已交付 —— 并行批量部署**：并发度 1-8（默认 1）、每设备实时进度、CSV/HTML 部署报告和失败设备一键重试。当前注册 20 个 QtTest/CTest 目标，以 `tests/CMakeLists.txt` 为准。

近期候选：

| 项目 | 优先级 | 说明 |
|------|--------|------|
| **真实 FTP/SFTP 集成验证** | 🔴 高 | 在可控真实设备上覆盖连接、上传、取消、并发与失败重试，保留可复查日志 |
| **长稳验证** | 🔴 高 | 按验收计划执行 16 台 50 轮并采集内存、句柄、线程与连接指标 |
| **发布自动化闭环** | 🔴 高 | 运行手动发布验收工作流并固化 artifacts/日志；正式 Release/Tag 仍由人工确认 |
| **SCP 部署上传** | 中 | 仅由真实设备需求驱动评估；SCP 无目录列表，不作为当前发布承诺 |

---

## 中期规划 (v2.8+)

| 项目 | 优先级 | 说明 |
|------|--------|------|
| **NetRelay 安全增强** | 中 | 非回环绑定模态确认弹窗、客户端来源 allowlist |
| **OPC UA 安全增强** | 中 | 按真实场景评估 Basic256Sha256、用户名/证书认证 |
| **ToolHost 多 Tool 并发** | 中 | 当前 DeviceForge 主窗口直接创建 Tool，待 ToolHost 支持多 Tool 并行管理 |
| **插件化加载 (QPluginLoader)** | 中 | DLL 动态加载 .dll Tool 插件，manifest.xml 入口点 |
| **跨平台 GUI** | 中 | Linux (X11/Wayland) 原生运行，DpapiCrypto 需替换为 libsecret |

---

## 长期愿景

| 方向 | 说明 |
|------|------|
| **工业级系统部署调试平台** | 覆盖各类工业设备的部署 → 测试 → 运维全流程，成为工业工程师的通用工具箱 |
| **社区生态** | 插件市场、模板分享、第三方 Tool 贡献机制 |

> 注：OPC UA 安全策略（Basic256/证书）、多语言国际化暂不作为优先方向，视社区反馈再定。

---

## 版本历史

| 版本 | 日期 | 里程碑 |
|------|------|--------|
| v2.0 | 2026-07-05 | DeviceForge 命名 + Tool 框架 + FTP/Telnet/Modbus/WebSocket |
| v2.1.0 | 2026-07-10 | NetRelayTool (中继+录制回放) + SSH 适配器 + 琴色主题 + OPC UA |
| v2.2.0 | 2026-07-18 | 在线更新 OTA + 远端预览重构 + CMake 标准构建 + 7 模块全绿 |
| v2.3.0 | 2026-07-24 | ConfigStore 本地持久化 (SQLite + DPAPI) + OPC UA 订阅卡死修复 + CI Debug SEGFAULT 修复 |
| v2.4 | 2026-07-25 | FTP 双栏重构 + 布局现代化（NavBar + 胶囊设备栏 + 可折叠日志）|
| v2.5 | 2026-07-26 | FTP 重命名/新建目录 + 远程精确对比 + SFTP 文件管理 + SylixOS 适配（EPSV/MULTICWD/递归删除）|
| v2.6 | 2026-08-07 | SFTP 批量部署（IDeployable 部署接口 + 部署循环协议化）+ 双栏面板模块化（FileBrowserPanel/IFileSource）+ 双主题/紧凑密度 |
| v2.7 | 2026-08-18 | UX 收尾：远程列表/连接异步化 + 面板源选择器 + 系统文件拖入上传恢复 + 顺带项（readdir 错误上报/sort 降序 SWO/像素验证测试）|
| v2.8.0 | 2026-08-22 | 并行批量部署（并发度 1-8）+ 每设备实时进度 + CSV/HTML 报告 + 失败设备一键重试；测试目标扩至 20 个 |

---

*路线图会根据社区反馈和实际需求动态调整。欢迎通过 GitHub Issues 提交功能建议。*
