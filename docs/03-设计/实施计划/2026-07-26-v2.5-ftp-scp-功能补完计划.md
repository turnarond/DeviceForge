# v2.5 FTP/SCP 功能补完实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 补完 FTP 双栏的 4 个功能缺口：远程重命名、新建目录、文件精确对比、SFTP 协议支持。

**Architecture:** FtpAdapter 新增 2 个 FTP 命令方法；RemoteFileModel 对比逻辑从文件名升级为 size 精确对比；SshAdapter 扩展 libssh2 SFTP 子系统（8 个文件操作）；FtpDeployWidget 新增协议下拉框 + 异步操作分流。

**Tech Stack:** Qt 6.11.1 (Widgets/Network/Concurrent), libcurl (FTP), libssh2 (SFTP), C++17

**Spec:** `docs/01-方案设计/2026-07-26-v2.5-ftp-scp-功能补完设计.md`

## Global Constraints

- Qt 6.11.1 + C++17，MSVC 2022
- 黑暗主题「琴色是动词」：琴色 #F0A030 仅信号态
- 类名 PascalCase、方法名 camelCase、成员变量 m_camelCase
- 版权头：`Copyright (c) 2024-2026 turnarond. All rights reserved.`
- libssh2 已纳入项目（SshAdapter 当前依赖），不新增第三方库
- 所有远程操作通过 QtConcurrent::run 异步执行
- 构建命令：`cd build && cmake --build . --config Release`
- 测试命令：`ctest -C Release --output-on-failure`

---

### Task 1: FtpAdapter 重命名 + 新建目录

**Files:**
- Modify: `src/adapter/FtpAdapter.h:33-37`（新增方法声明）
- Modify: `src/adapter/FtpAdapter.cpp:510`（新增方法实现）

**Interfaces:**
- Consumes: 现有 FtpAdapter::deleteFile() 的 CURLOPT_QUOTE 模式
- Produces:
  ```cpp
  bool FtpAdapter::renameFile(const std::string& remotePath, const std::string& newName);
  bool FtpAdapter::makeDirectory(const std::string& remotePath);
  ```

- [ ] **Step 1: 在 FtpAdapter.h 添加声明**

在 `deleteDirectory()` 声明后添加：
```cpp
// 重命名远程文件/目录（FTP RNFR + RNTO 命令）
bool renameFile(const std::string& remotePath, const std::string& newName);
// 新建远程目录（FTP MKD 命令）
bool makeDirectory(const std::string& remotePath);
```

- [ ] **Step 2: 在 FtpAdapter.cpp 实现 renameFile**

在 `deleteDirectory()` 实现后添加：
```cpp
bool FtpAdapter::renameFile(const std::string& remotePath, const std::string& newName)
{
    CURL* curl = curl_easy_init();
    if (!curl) {
        m_impl->m_lastError = "curl_easy_init() 失败";
        return false;
    }

    // 使用父目录 URL（RNFR/RNTO 操作基于连接当前目录或绝对路径）
    std::string url = m_impl->buildUrl(remotePath);
    m_impl->setupCommonOpts(curl, url);

    // 确保路径以 / 开头（FTP 命令要求绝对路径）
    std::string path = remotePath;
    if (!path.empty() && path[0] != '/') path = "/" + path;
    std::string newPath = newName;
    if (!newPath.empty() && newPath[0] != '/') newPath = "/" + newPath;

    struct curl_slist* commands = nullptr;
    commands = curl_slist_append(commands, ("RNFR " + path).c_str());
    commands = curl_slist_append(commands, ("RNTO " + newPath).c_str());
    curl_easy_setopt(curl, CURLOPT_QUOTE, commands);

    CURLcode res = curl_easy_perform(curl);
    curl_slist_free_all(commands);
    curl_easy_cleanup(curl);

    if (res == CURLE_OK) {
        m_impl->m_lastError.clear();
        return true;
    }
    m_impl->m_lastError = std::string("重命名失败: ") + curl_easy_strerror(res);
    return false;
}
```

- [ ] **Step 3: 在 FtpAdapter.cpp 实现 makeDirectory**

```cpp
bool FtpAdapter::makeDirectory(const std::string& remotePath)
{
    CURL* curl = curl_easy_init();
    if (!curl) {
        m_impl->m_lastError = "curl_easy_init() 失败";
        return false;
    }

    std::string url = m_impl->buildUrl(remotePath);
    m_impl->setupCommonOpts(curl, url);

    std::string path = remotePath;
    if (!path.empty() && path[0] != '/') path = "/" + path;

    struct curl_slist* commands = nullptr;
    commands = curl_slist_append(commands, ("MKD " + path).c_str());
    curl_easy_setopt(curl, CURLOPT_QUOTE, commands);

    CURLcode res = curl_easy_perform(curl);
    curl_slist_free_all(commands);
    curl_easy_cleanup(curl);

    if (res == CURLE_OK) {
        m_impl->m_lastError.clear();
        return true;
    }
    m_impl->m_lastError = std::string("新建目录失败: ") + curl_easy_strerror(res);
    return false;
}
```

- [ ] **Step 4: 编译验证 + Commit**

```bash
cd build && cmake --build . --config Release
ctest -C Release --output-on-failure
```

```bash
git add src/adapter/FtpAdapter.h src/adapter/FtpAdapter.cpp
git commit -m "feat(ftp): FtpAdapter 新增 renameFile + makeDirectory (RNFR/RNTO/MKD)"
```

---

### Task 2: LocalFileInfo + RemoteFileModel 精确对比

**Files:**
- Modify: `src/tools/FtpDeployTool/FtpFileInfo.h`（新增 LocalFileInfo 结构体）
- Modify: `src/tools/FtpDeployTool/RemoteFileModel.h:34`（setLocalFilesForCompare 签名升级）
- Modify: `src/tools/FtpDeployTool/RemoteFileModel.cpp:45-61`（精确对比逻辑）
- Modify: `src/tools/FtpDeployTool/FtpDeployWidget.cpp`（触发精确对比的调用）

**Interfaces:**
- Consumes: FtpFileInfo.h (Task 1 不变部分)
- Produces:
  ```cpp
  // FtpFileInfo.h 新增
  struct LocalFileInfo {
      std::string name;
      uint64_t    size = 0;
      std::string dateTime;
  };

  // RemoteFileModel.h 签名变更
  void setLocalFilesForCompare(const std::vector<LocalFileInfo>& localFiles);
  ```

- [ ] **Step 1: 在 FtpFileInfo.h 添加 LocalFileInfo**

在 `FtpFileInfo` 结构体定义后添加：
```cpp
// 本地文件信息 — 用于与远程文件进行精确对比（size + 时间戳）
struct LocalFileInfo {
    std::string name;
    uint64_t    size = 0;
    std::string dateTime;  // ISO 8601
};
```

- [ ] **Step 2: 更新 RemoteFileModel.h 签名**

```cpp
// 旧:
void setLocalFilesForCompare(const std::vector<std::string>& localFileNames);
// 新:
void setLocalFilesForCompare(const std::vector<struct LocalFileInfo>& localFiles);
```

同步更新成员变量：
```cpp
// 旧:
std::set<std::string> m_localFileNames;
// 新:
std::map<std::string, LocalFileInfo> m_localFiles; // name → info
```

- [ ] **Step 3: 更新 RemoteFileModel.cpp 对比逻辑**

```cpp
void RemoteFileModel::setLocalFilesForCompare(const std::vector<LocalFileInfo>& localFiles)
{
    m_localFiles.clear();
    for (const auto& f : localFiles) m_localFiles[f.name] = f;

    // 精确对比：同名 + 同 size → 绿色（已同步）
    //           同名 + 异 size → 黄色（有差异）
    m_syncedNames.clear();
    m_diffNames.clear();
    for (const auto& f : m_files) {
        auto it = m_localFiles.find(f.name);
        if (it != m_localFiles.end()) {
            if (it->second.size == f.size) {
                m_syncedNames.insert(f.name);  // 绿色
            } else {
                m_diffNames.insert(f.name);    // 黄色
            }
        }
    }
    if (!m_files.empty()) {
        emit dataChanged(index(0, 0),
            index(static_cast<int>(m_files.size()) - 1, ColCount - 1));
    }
}
```

同步更新 `setFileList()` 中的对比重建（调用 `m_localFiles` 替代 `m_localFileNames`）。

- [ ] **Step 4: FtpDeployWidget 触发精确对比**

在本地面板选中变更时，构造 LocalFileInfo 列表传入 RemoteFileModel。在 `FtpDeployWidget` 中连接 `m_localTree->selectionModel()` 的 `selectionChanged` 信号：

```cpp
connect(m_localTree->selectionModel(), &QItemSelectionModel::selectionChanged,
    this, [this]() {
        std::vector<LocalFileInfo> locals;
        QModelIndexList sel = m_localTree->selectionModel()->selectedRows();
        for (const auto& idx : sel) {
            QFileInfo fi = m_localFsModel->fileInfo(idx);
            LocalFileInfo lfi;
            lfi.name = fi.fileName().toStdString();
            lfi.size = fi.size();
            lfi.dateTime = fi.lastModified().toString(Qt::ISODate).toStdString();
            locals.push_back(lfi);
        }
        m_remoteModel->setLocalFilesForCompare(locals);
    });
```

- [ ] **Step 5: 编译验证 + Commit**

```bash
cd build && cmake --build . --config Release
ctest -C Release --output-on-failure
```

```bash
git add src/tools/FtpDeployTool/FtpFileInfo.h \
        src/tools/FtpDeployTool/RemoteFileModel.h \
        src/tools/FtpDeployTool/RemoteFileModel.cpp \
        src/tools/FtpDeployTool/FtpDeployWidget.cpp
git commit -m "feat(ftp): RemoteFileModel 精确对比 — size 对比替代文件名匹配"
```

---

### Task 3: SshAdapter SFTP 扩展

**Files:**
- Modify: `src/adapter/SshAdapter.h`（新增 SFTP 方法声明 + m_sftpSession 成员）
- Modify: `src/adapter/SshAdapter.cpp`（新增 SFTP 8 个方法实现）

**Interfaces:**
- Consumes: 现有 SshAdapter libssh2 session 管理（connect/disconnect）
- Produces:
  ```cpp
  class SshAdapter : public IProtocolAdapter {
  public:
      // 新增 SFTP 文件操作（8 个方法）
      std::vector<struct FtpFileInfo> sftpListDirectory(const std::string& remotePath);
      bool sftpUploadFile(const std::string& localPath, const std::string& remotePath);
      bool sftpDownloadFile(const std::string& remotePath, const std::string& localPath);
      bool sftpDeleteFile(const std::string& remotePath);
      bool sftpDeleteDirectory(const std::string& remotePath);
      bool sftpRename(const std::string& oldPath, const std::string& newPath);
      bool sftpMakeDirectory(const std::string& remotePath);
      void sftpSetProgressCallback(std::function<void(int)> cb);
  private:
      LIBSSH2_SFTP* m_sftpSession = nullptr;  // disconnect 时 shutdown
      bool sftpInit();                         // connect 后调用
      std::function<void(int)> m_sftpProgressCb;
  };
  ```

- [ ] **Step 1: 更新 SshAdapter.h — 新增声明**

在 `#include <libssh2/libssh2.h>` 后添加：
```cpp
#include <libssh2/libssh2_sftp.h>  // SFTP subsystem
#include "tools/FtpDeployTool/FtpFileInfo.h"  // 复用 FtpFileInfo
#include <functional>
```

在 public 区域 `capability()` 声明后添加 8 个 SFTP 方法声明：
```cpp
// --- SFTP 文件操作（SSH 文件传输子系统）---
std::vector<FtpFileInfo> sftpListDirectory(const std::string& remotePath);
bool sftpUploadFile(const std::string& localPath, const std::string& remotePath);
bool sftpDownloadFile(const std::string& remotePath, const std::string& localPath);
bool sftpDeleteFile(const std::string& remotePath);
bool sftpDeleteDirectory(const std::string& remotePath);
bool sftpRename(const std::string& oldPath, const std::string& newPath);
bool sftpMakeDirectory(const std::string& remotePath);
void sftpSetProgressCallback(std::function<void(int)> cb);
```

在 private 区域添加：
```cpp
LIBSSH2_SFTP* m_sftpSession = nullptr;
std::function<void(int)> m_sftpProgressCb;
bool sftpInit();
```

- [ ] **Step 2: 实现 sftpInit + connect 集成**

```cpp
#include "tools/FtpDeployTool/FtpListParser.h" // 不需要——SFTP 返回结构化数据

bool SshAdapter::sftpInit()
{
    if (!m_session) return false;
    m_sftpSession = libssh2_sftp_init(m_session);
    if (!m_sftpSession) {
        m_lastError = "SFTP 初始化失败";
        return false;
    }
    return true;
}
```

修改 `connect()` 方法——在认证成功后调用 `sftpInit()`，失败不影响连接（SFTP 是可选子系统）：
```cpp
// connect() 末尾添加:
if (!sftpInit()) {
    // SFTP 子系统不可用（服务器可能未启用），
    // 不影响 SSH 连接本身（exec channel 仍可用）
}
```

- [ ] **Step 3: disconnect 中 shutdown SFTP**

```cpp
void SshAdapter::disconnect()
{
    if (m_sftpSession) {
        libssh2_sftp_shutdown(m_sftpSession);
        m_sftpSession = nullptr;
    }
    // ... 现有 cleanup 代码不变
}
```

- [ ] **Step 4: 实现 sftpListDirectory**

```cpp
std::vector<FtpFileInfo> SshAdapter::sftpListDirectory(const std::string& remotePath)
{
    std::vector<FtpFileInfo> result;
    if (!m_sftpSession) { m_lastError = "SFTP 未初始化"; return result; }

    LIBSSH2_SFTP_HANDLE* dir = libssh2_sftp_opendir(m_sftpSession, remotePath.c_str());
    if (!dir) {
        m_lastError = "SFTP 打开目录失败: " + remotePath;
        return result;
    }

    char filename[512];
    LIBSSH2_SFTP_ATTRIBUTES attrs;
    while (libssh2_sftp_readdir(dir, filename, sizeof(filename), &attrs) > 0) {
        std::string name(filename);
        if (name == "." || name == "..") continue;

        FtpFileInfo fi;
        fi.name = name;
        fi.isDir = (attrs.flags & LIBSSH2_SFTP_ATTR_PERMISSIONS)
            ? S_ISDIR(attrs.permissions) : false;
        fi.size = (attrs.flags & LIBSSH2_SFTP_ATTR_SIZE) ? attrs.filesize : 0;
        fi.dateTime = (attrs.flags & LIBSSH2_SFTP_ATTR_ACMODTIME)
            ? QDateTime::fromSecsSinceEpoch(attrs.mtime).toString("yyyy-MM-dd HH:mm:ss").toStdString()
            : "";

        // 权限：八进制 → rwx 字符串
        if (attrs.flags & LIBSSH2_SFTP_ATTR_PERMISSIONS) {
            unsigned int p = attrs.permissions & 0777;
            fi.permissions = std::string()
                + ((p & 0400) ? "r" : "-") + ((p & 0200) ? "w" : "-") + ((p & 0100) ? "x" : "-")
                + ((p & 0040) ? "r" : "-") + ((p & 0020) ? "w" : "-") + ((p & 0010) ? "x" : "-")
                + ((p & 0004) ? "r" : "-") + ((p & 0002) ? "w" : "-") + ((p & 0001) ? "x" : "-");
        }

        result.push_back(fi);
    }
    libssh2_sftp_closedir(dir);
    return result;
}
```

- [ ] **Step 5: 实现 sftpUploadFile + sftpDownloadFile**

```cpp
bool SshAdapter::sftpUploadFile(const std::string& localPath, const std::string& remotePath)
{
    if (!m_sftpSession) { m_lastError = "SFTP 未初始化"; return false; }

    // 打开本地文件读取
    FILE* localFile = fopen(localPath.c_str(), "rb");
    if (!localFile) { m_lastError = "无法打开本地文件: " + localPath; return false; }
    fseek(localFile, 0, SEEK_END);
    uint64_t fileSize = ftell(localFile);
    fseek(localFile, 0, SEEK_SET);

    // 创建远程文件
    LIBSSH2_SFTP_HANDLE* remoteFile = libssh2_sftp_open(m_sftpSession, remotePath.c_str(),
        LIBSSH2_FXF_WRITE | LIBSSH2_FXF_CREAT | LIBSSH2_FXF_TRUNC,
        LIBSSH2_SFTP_S_IRUSR | LIBSSH2_SFTP_S_IWUSR |
        LIBSSH2_SFTP_S_IRGRP | LIBSSH2_SFTP_S_IROTH);
    if (!remoteFile) { fclose(localFile); m_lastError = "SFTP 创建文件失败"; return false; }

    // 分块上传（8KB 缓冲区）
    char buf[8192];
    uint64_t sent = 0;
    bool ok = true;
    while (sent < fileSize) {
        size_t n = fread(buf, 1, sizeof(buf), localFile);
        if (n == 0) break;
        ssize_t rc = libssh2_sftp_write(remoteFile, buf, n);
        if (rc < 0) { ok = false; break; }
        sent += rc;
        if (m_sftpProgressCb && fileSize > 0) {
            int pct = static_cast<int>((sent * 100) / fileSize);
            m_sftpProgressCb(pct);
        }
    }

    fclose(localFile);
    libssh2_sftp_close(remoteFile);
    if (!ok) m_lastError = "SFTP 上传写入失败";
    return ok;
}

bool SshAdapter::sftpDownloadFile(const std::string& remotePath, const std::string& localPath)
{
    if (!m_sftpSession) { m_lastError = "SFTP 未初始化"; return false; }

    LIBSSH2_SFTP_HANDLE* remoteFile = libssh2_sftp_open(m_sftpSession, remotePath.c_str(),
        LIBSSH2_FXF_READ, 0);
    if (!remoteFile) { m_lastError = "SFTP 打开文件失败"; return false; }

    // 获取文件大小
    LIBSSH2_SFTP_ATTRIBUTES attrs;
    libssh2_sftp_fstat(remoteFile, &attrs);
    uint64_t fileSize = attrs.filesize;

    FILE* localFile = fopen(localPath.c_str(), "wb");
    if (!localFile) { libssh2_sftp_close(remoteFile); m_lastError = "无法创建本地文件"; return false; }

    char buf[8192];
    uint64_t received = 0;
    bool ok = true;
    while (true) {
        ssize_t n = libssh2_sftp_read(remoteFile, buf, sizeof(buf));
        if (n < 0) { ok = false; break; }
        if (n == 0) break;
        fwrite(buf, 1, n, localFile);
        received += n;
        if (m_sftpProgressCb && fileSize > 0) {
            int pct = static_cast<int>((received * 100) / fileSize);
            m_sftpProgressCb(pct);
        }
    }

    fclose(localFile);
    libssh2_sftp_close(remoteFile);
    if (!ok) m_lastError = "SFTP 下载读取失败";
    return ok;
}
```

- [ ] **Step 6: 实现 sftpDeleteFile / sftpDeleteDirectory**

```cpp
bool SshAdapter::sftpDeleteFile(const std::string& remotePath)
{
    if (!m_sftpSession) { m_lastError = "SFTP 未初始化"; return false; }
    int rc = libssh2_sftp_unlink(m_sftpSession, remotePath.c_str());
    if (rc != 0) { m_lastError = "SFTP 删除文件失败"; return false; }
    return true;
}

bool SshAdapter::sftpDeleteDirectory(const std::string& remotePath)
{
    if (!m_sftpSession) { m_lastError = "SFTP 未初始化"; return false; }

    // 先尝试直接删除（空目录），失败则递归删除
    if (libssh2_sftp_rmdir(m_sftpSession, remotePath.c_str()) == 0) return true;

    // 目录非空：列出内容，递归删除
    auto entries = sftpListDirectory(remotePath);
    for (const auto& e : entries) {
        std::string fullPath = remotePath + "/" + e.name;
        if (e.isDir) sftpDeleteDirectory(fullPath);
        else sftpDeleteFile(fullPath);
    }
    int rc = libssh2_sftp_rmdir(m_sftpSession, remotePath.c_str());
    if (rc != 0) { m_lastError = "SFTP 删除目录失败"; return false; }
    return true;
}
```

- [ ] **Step 7: 实现 sftpRename + sftpMakeDirectory**

```cpp
bool SshAdapter::sftpRename(const std::string& oldPath, const std::string& newPath)
{
    if (!m_sftpSession) { m_lastError = "SFTP 未初始化"; return false; }
    int rc = libssh2_sftp_rename(m_sftpSession, oldPath.c_str(), newPath.c_str());
    if (rc != 0) { m_lastError = "SFTP 重命名失败"; return false; }
    return true;
}

bool SshAdapter::sftpMakeDirectory(const std::string& remotePath)
{
    if (!m_sftpSession) { m_lastError = "SFTP 未初始化"; return false; }
    int rc = libssh2_sftp_mkdir(m_sftpSession, remotePath.c_str(),
        LIBSSH2_SFTP_S_IRWXU | LIBSSH2_SFTP_S_IRGRP | LIBSSH2_SFTP_S_IXGRP | LIBSSH2_SFTP_S_IROTH | LIBSSH2_SFTP_S_IXOTH);
    if (rc != 0) { m_lastError = "SFTP 新建目录失败"; return false; }
    return true;
}

void SshAdapter::sftpSetProgressCallback(std::function<void(int)> cb)
{
    m_sftpProgressCb = std::move(cb);
}
```

- [ ] **Step 8: 编译验证 + Commit**

```bash
cd build && cmake --build . --config Release
```
注意：新增 `#include <libssh2/libssh2_sftp.h>` 需确认 CMake 的 include 路径已包含 libssh2 头文件目录（现有 SshAdapter 已链接 libssh2，路径应已就绪）。

```bash
git add src/adapter/SshAdapter.h src/adapter/SshAdapter.cpp
git commit -m "feat(sftp): SshAdapter SFTP 扩展 — 8 个文件操作（list/upload/download/delete/rename/mkdir）"
```

---

### Task 4: FtpDeployWidget 协议切换 + 重命名/新建目录集成 + SCP 端口适配

**Files:**
- Modify: `src/tools/FtpDeployTool/FtpDeployWidget.h`（新增 m_protocolCombo + 协议分流辅助方法）
- Modify: `src/tools/FtpDeployTool/FtpDeployWidget.cpp`（工具栏添加协议选择 + rename/newdir 实际调用 + 异步分流）

**Interfaces:**
- Consumes: FtpAdapter (Task 1), RemoteFileModel (Task 2), SshAdapter (Task 3)
- Produces: 双栏协议切换 UI + 完整远程文件管理（FTP & SFTP）

- [ ] **Step 1: 在 FtpDeployWidget.h 添加新成员**

```cpp
QComboBox* m_protocolCombo = nullptr;  // 协议选择：FTP / SFTP

// 辅助方法：根据当前协议获取 Adapter 类型标识
std::string currentProtocol() const;   // "ftp" or "ssh"
```

- [ ] **Step 2: 在 setupToolbar 中添加协议下拉框**

在工具栏第一行的最前面插入：
```cpp
// 协议选择
toolbar->addWidget(new QLabel("协议:", this), 0, 0);
m_protocolCombo = new QComboBox(this);
m_protocolCombo->addItem("FTP");
m_protocolCombo->addItem("SFTP");
m_protocolCombo->setFixedWidth(80);
connect(m_protocolCombo, &QComboBox::currentTextChanged, this, [this](const QString& proto) {
    m_remoteModel->clear();
    m_currentRemotePath = "/";
    refreshBreadcrumb("/");
    m_remotePathEdit->setText("/");
    if (proto == "SFTP") {
        m_portSpin->setValue(22);
    } else {
        m_portSpin->setValue(21);
    }
    onRefreshRemote();
});
toolbar->addWidget(m_protocolCombo, 0, 1);
```

- [ ] **Step 3: 异步操作分流**

在 `onRefreshRemote()` / `onDeleteRemote()` / `onDownloadRemote()` 中，将所有 `ProtocolRegistry::instance()->create("ftp")` 替换为分流逻辑：

```cpp
// 辅助 lambda：创建当前协议的 Adapter
auto createAdapter = [this]() -> std::pair<std::unique_ptr<IProtocolAdapter>, int> {
    if (m_protocolCombo->currentText() == "SFTP") {
        return {ProtocolRegistry::instance()->create("ssh"), 22};
    }
    return {ProtocolRegistry::instance()->create("ftp"), 21};
};

// 在 onRefreshRemote 中使用：
QtConcurrent::run([this, path, user, pass, useFtps, port, deviceIp, proto = m_protocolCombo->currentText().toStdString()]() {
    auto [adapter, defaultPort] = [&proto]() -> std::pair<...> {
        if (proto == "SFTP") return {ProtocolRegistry::instance()->create("ssh"), 22};
        return {ProtocolRegistry::instance()->create("ftp"), 21};
    }();
    // ... 根据 proto 分流:
    if (proto == "SFTP") {
        auto* ssh = dynamic_cast<SshAdapter*>(adapter.get());
        auto files = ssh->sftpListDirectory(path.toStdString());
        // ...
    } else {
        auto* ftp = dynamic_cast<FtpAdapter*>(adapter.get());
        auto files = ftp->listDirectoryParsed(path.toStdString());
        // ...
    }
});
```

对所有异步操作（onRefreshRemote / onDeleteRemote / onDownloadRemote）做相同的分流处理。拖拽上传同理。

- [ ] **Step 4: 实现 onRenameRemote + onNewRemoteDir 实际功能**

将 Task 1 的 FtpAdapter::renameFile/makeDirectory 接入：

```cpp
void FtpDeployWidget::onRenameRemote()
{
    // ... 获取 oldName 和新名称 newName（现有代码不变，直到 QInputDialog 返回）
    if (!ok || newName.isEmpty() || newName == oldName) return;

    // 异步执行重命名
    const QString proto = m_protocolCombo->currentText();
    const QString old = oldName, nu = newName;
    const QString deviceIp = m_deviceCombo->currentText();
    const int port = m_portSpin->value();
    const std::string user = m_deviceBus ? m_deviceBus->user().toStdString() : "";
    const std::string pass = m_deviceBus ? m_deviceBus->password().toStdString() : "";
    const std::string remotePath = m_currentRemotePath.toStdString();

    QtConcurrent::run([this, proto, old, nu, deviceIp, port, user, pass, remotePath]() {
        auto adapter = ProtocolRegistry::instance()->create(proto == "SFTP" ? "ssh" : "ftp");
        DeviceInfo dev; dev.ip = deviceIp.toStdString(); dev.port = port;
        AuthInfo auth; auth.user = user; auth.password = pass;

        std::string oldFull = remotePath + "/" + old.toStdString();
        std::string newFull = nu.toStdString(); // RNTO 只需新文件名，不需完整路径
        bool ok = false;
        if (proto == "SFTP") {
            if (auto* ssh = dynamic_cast<SshAdapter*>(adapter.get())) {
                ssh->connect(dev, auth);
                ok = ssh->sftpRename(oldFull, newFull);
                ssh->disconnect();
            }
        } else {
            if (auto* ftp = dynamic_cast<FtpAdapter*>(adapter.get())) {
                ftp->connect(dev, auth);
                ok = ftp->renameFile(oldFull, newFull);
                ftp->disconnect();
            }
        }
        QMetaObject::invokeMethod(this, [this, old, nu, ok]() {
            appendLog(ok ? QString("已重命名: %1 → %2").arg(old, nu)
                         : QString("重命名失败: %1").arg(old));
            if (ok) onRefreshRemote();
        }, Qt::QueuedConnection);
    });
}
```

`onNewRemoteDir()` 同理——改为调用 `ftp->makeDirectory()` 或 `ssh->sftpMakeDirectory()`。

- [ ] **Step 5: 编译验证 + 测试 + Commit**

```bash
cd build && cmake --build . --config Release
ctest -C Release --output-on-failure
```

```bash
git add src/tools/FtpDeployTool/FtpDeployWidget.h \
        src/tools/FtpDeployTool/FtpDeployWidget.cpp
git commit -m "feat(ftp): FtpDeployWidget 协议切换 + 重命名/新建目录/分流实现"
```

---

### Task 5: 版本号 + 文档同步 + 集成验证

**Files:**
- Modify: `CMakeLists.txt:4`（VERSION 2.4.0 → 2.5.0）
- Modify: `DeployMaster.rc:25-26,43,48`（2.4 → 2.5）
- Modify: `CLAUDE.md:97`（当前版本 2.4.0 → 2.5.0）
- Modify: `docs/ROADMAP.md`（v2.5 条目标记完成 + 新增版本历史行）
- Create: `docs/articles/2026-07-26-deviceforge-v2.5-功能补完.md`（可选）

- [ ] **Step 1: 更新所有版本号**

三个文件同步更改为 2.5.0：
- `CMakeLists.txt:4` — `VERSION 2.4.0` → `VERSION 2.5.0`
- `DeployMaster.rc` — `2,4,0,0` → `2,5,0,0`, `"2.4.0.0"` → `"2.5.0.0"`
- `CLAUDE.md:97` — "当前版本 2.4.0" → "当前版本 2.5.0"

```bash
sed -i 's/VERSION 2.4.0/VERSION 2.5.0/' CMakeLists.txt
python -c "data=open('DeployMaster.rc','rb').read().decode('utf-16-le');open('DeployMaster.rc','wb').write(data.replace('2,4,0,0','2,5,0,0').replace('2.4.0.0','2.5.0.0').encode('utf-16-le'))"
sed -i 's/当前版本 2.4.0/当前版本 2.5.0/' CLAUDE.md
```

- [ ] **Step 2: 更新 CLAUDE.md 内容**

更新"已完成"部分——新增 v2.5 的功能描述：
```markdown
- **FTP 远程文件管理**：重命名（RNFR/RNTO）+ 新建目录（MKD）+ 精确对比（size 对比替代文件名匹配）| v2.5
- **SFTP 文件传输**：SshAdapter SFTP 扩展，双栏文件管理器同时支持 FTP 和 SFTP 两种协议 | v2.5
```

更新"短期规划"——移除已完成的 FTP TODO 项，将"SCP 支持"状态从"已取消"改为"已完成（v2.5 SFTP）"。

- [ ] **Step 3: 更新 ROADMAP.md**

v2.5 版本历史行：
```markdown
| v2.5 | 2026-07-26 | FTP 重命名/新建目录 + 远程精确对比 + SFTP 协议支持（双栏 FTP/SFTP 切换）|
```

短期规划中已完成项标记为完成。

- [ ] **Step 4: 编译 + 全测试**

```bash
cd build
cmake .. -G "Visual Studio 17 2022" -A x64 -DCMAKE_PREFIX_PATH="c:\Qt\6.11.1\msvc2022_64"
cmake --build . --config Release
ctest -C Release --output-on-failure
```

验证：0 错误，5/5 测试通过。

- [ ] **Step 5: Git 状态检查 + Commit**

```bash
git status --short  # 确保工作区干净
```

```bash
git add -A
git commit -m "chore: v2.5.0 — 版本号 + 文档同步 + 集成验证通过"
```

---

### 自审检查

1. **Spec coverage**: FTP重命名新建目录(T1) / 精确对比(T2) / SFTP扩展(T3) / 协议切换+集成(T4) / 版本号文档(T5) — 全部覆盖
2. **Placeholder scan**: 无 TBD/TODO，所有代码块完整
3. **Type consistency**: `LocalFileInfo` 在 T2 定义，T2 消费；`sftpXxx()` 方法在 T3 定义，T4 消费；`FtpFileInfo` 在 T1 和 T3 中共享使用，类型一致
4. **依赖顺序**: T1-T3 独立可并行；T4 依赖 T1+T2+T3；T5 依赖全部
