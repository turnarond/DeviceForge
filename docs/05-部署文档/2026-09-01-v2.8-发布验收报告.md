# DeviceForge v2.8 发布验收报告

**建档日期**：2026-09-01
**最近补录**：2026-09-04（Asia/Shanghai）
**状态**：发布验收补录中

## 1. 验收基线与口径

- 代码基线：`origin/main` 的 `a6e9283af01489924db9f8209d192f8eecef2714`。
- 发布标记：`v2.8.0` Tag 指向 `a6e9283`，本报告不移动或重建 Tag。
- 本地 Qt：`D:\Qt\6.11.1\msvc2022_64`。
- 本地编译环境：Windows、Visual Studio 2026 Community Developer Command Prompt v18.8.0、MSVC 19.51.36248、Ninja；`build_verify_debug` 与 `build_verify_release` 为 Debug/Release 单配置目录，均使用 `--parallel 2`。本机 VS2022 Professional 未安装 C++ 编译器，因此本地结果不替代 GitHub Actions 的 VS2022 验证。
- 本地 Python：3.9.10，实际解释器为 `D:\Qt\Tools\mingw1310_64\opt\bin\python3.exe`；不使用 WindowsApps 占位入口。
- 状态口径：提交存在只能证明“已实现”；命令日志证明“自动验证”；GUI、现场协议与远端工作流必须有独立记录；长稳必须有轮次和资源基线。无证据项保持“未执行”。

## 2. 实现证据

| 项目 | 命令或入口 | 环境 | 时间 | 结果 | 证据或阻塞 |
|---|---|---|---|---|---|
| DeployReport 与 CSV/HTML 渲染 | `git show --stat a118fef` | Git 历史 | 2026-08-22 | 已实现 | `a118fef`：源码与 `tst_deploy_report` 同提交 |
| DeployJob 串行等价抽取 | `git show --stat 25a8623` | Git 历史 | 2026-08-22 | 已实现 | `25a8623` |
| DeploymentRunner 并发与取消传播 | `git show --stat 32b11fc` | Git 历史 | 2026-08-22 | 已实现 | `32b11fc`：源码与 `tst_deploy_runner` 同提交 |
| 每设备进度与 Backend 接线 | `git show --stat d963e8b` | Git 历史 | 2026-08-23 | 已实现 | `d963e8b` |
| 并发度设置、报告导出、失败重试 | `git show --stat 814654a` | Git 历史 | 2026-08-23 | 已实现 | `814654a` |
| NSIS 脚本与版本同步 | `git show --stat 2f6c002` | Git 历史 | 2026-08-23 | 已实现 | `2f6c002`；仅证明脚本存在，不证明本地安装包已生成 |
| 终态显示、总进度与报告写盘修复 | `git show --stat 3cb6687` | Git 历史 | 2026-08-23 | 已实现 | `3cb6687` |
| v2.8 合并与 Tag | `git tag --points-at a6e9283` | Git 历史 | 2026-08-23 | 已确认 | 合并提交 `a6e9283`；命令输出 `v2.8.0` |

## 3. 自动验证

| 项目 | 命令或入口 | 环境 | 时间 | 结果 | 证据或阻塞 |
|---|---|---|---|---|---|
| Release Ninja 构建 | 2026-09-01 独立 Release 构建记录 | Windows；VS2026 MSVC/Ninja；Qt `D:\Qt\6.11.1\msvc2022_64` | 2026-09-01 | 通过 | 本轮任务 brief 固化的独立验证结论；构建成功 |
| CTest 全量 | `ctest --test-dir build_release --output-on-failure` | 正常 Windows 用户；同一 Release 二进制 | 2026-09-01 | 20/20 通过 | 本轮任务 brief 固化的正常用户独立验证结论 |
| Python 工具测试（当日基线） | `D:\Qt\Tools\mingw1310_64\opt\bin\python3.exe -m unittest discover -s tools/tests -v` | Python 3.9.10 | 2026-09-01 | 17/17 通过 | 2026-09-01 独立验证记录；是当日旧测试集，不替代当前数量 |
| 旧版版本一致性检查 | `D:\Qt\Tools\mingw1310_64\opt\bin\python3.exe tools/devtools/versioncheck.py` | Python 3.9.10 | 2026-09-01 | 通过 | 2026-09-01 独立验证记录；该工具当时尚未扩展至九个强制来源 |
| DPAPI（隔离账户） | `tst_dpapi_crypto.exe -v1` | 缺少正常用户配置文件的隔离账户；同一 Release 二进制 | 2026-09-01 | 2/3，错误 | Windows 错误 2/3；属于账户环境差异，不能作为产品通过证据 |
| DPAPI（正常用户复测） | `tst_dpapi_crypto.exe -v1` | 正常 Windows 用户配置文件；同一 Release 二进制 | 2026-09-01 | 4/4 通过 | 与隔离账户使用同一二进制，正常用户上下文复测 |
| 强版本一致性检查 | `D:\Qt\Tools\mingw1310_64\opt\bin\python3.exe tools/devtools/versioncheck.py` | Python 3.9.10 | 2026-09-03 | 通过，退出 0 | Task 4 报告及 Task 5 复跑：九个强制来源均为 `2.8.0` |
| Python 工具测试（当前测试集） | `D:\Qt\Tools\mingw1310_64\opt\bin\python3.exe -m unittest discover -s tools/tests -v` | Python 3.9.10 | 2026-09-03 | 25/25 通过，退出 0 | Task 4 报告及 Task 5 复跑（0.714 秒）；当前数量与 2026-09-01 的 17 项基线分开记录 |
| Python 工具测试（交付复跑） | `D:\Qt\Tools\mingw1310_64\opt\bin\python3.exe -m unittest discover -s tools/tests -v` | Python 3.9.10 | 2026-09-04 11:33:34 +08:00 | 25/25 通过，退出 0 | unittest 报告运行 0.800 秒；进程计时 1.083 秒 |
| 强版本一致性检查（交付复跑） | `D:\Qt\Tools\mingw1310_64\opt\bin\python3.exe tools/devtools/versioncheck.py` | Python 3.9.10 | 2026-09-04 11:33:44 +08:00 | 通过，退出 0 | 九个强制来源均为 `2.8.0`；进程计时 0.097 秒 |
| Debug Ninja 配置与构建 | `VsDevCmd.bat -arch=amd64 -host_arch=amd64` 后执行 `cmake -S . -B build_verify_debug -G Ninja -DCMAKE_BUILD_TYPE=Debug -DCMAKE_PREFIX_PATH="D:\Qt\6.11.1\msvc2022_64"`、`cmake --build build_verify_debug --parallel 2` | VS2026 Community v18.8.0；MSVC 19.51.36248；Qt 6.11.1 | 2026-09-04 11:34:19 / 11:36:58 +08:00 | 配置、构建均通过，退出 0 | 配置 9.994 秒；Ninja 185/185，构建 146.692 秒 |
| Debug CTest 全量 | `PATH=D:\Qt\6.11.1\msvc2022_64\bin;%PATH%` 后执行 `ctest --test-dir build_verify_debug --output-on-failure` | 正常 Windows 用户 profile；同一 Debug 二进制 | 2026-09-04 11:39:44 +08:00 | 20/20 通过，退出 0 | CTest 40.51 秒；`tst_dpapi_crypto` 通过 |
| Release Ninja 配置与构建 | `VsDevCmd.bat -arch=amd64 -host_arch=amd64` 后执行 `cmake -S . -B build_verify_release -G Ninja -DCMAKE_BUILD_TYPE=Release -DCMAKE_PREFIX_PATH="D:\Qt\6.11.1\msvc2022_64"`、`cmake --build build_verify_release --parallel 2` | VS2026 Community v18.8.0；MSVC 19.51.36248；Qt 6.11.1 | 2026-09-04 11:40:05 / 11:44:18 +08:00 | 配置、构建均通过，退出 0 | 配置 8.162 秒；Ninja 185/185，构建 243.061 秒 |
| Release CTest 全量 | `PATH=D:\Qt\6.11.1\msvc2022_64\bin;%PATH%` 后执行 `ctest --test-dir build_verify_release --output-on-failure` | 正常 Windows 用户 profile；同一 Release 二进制 | 2026-09-04 12:17:41 +08:00 | 20/20 通过，退出 0 | CTest 9.00 秒；`tst_dpapi_crypto` 通过 |
| Qt 部署参数诊断 | `windeployqt --release --plugindir build_verify_release\plugins --include-plugins platforms,sqldrivers build_verify_release\DeviceForge.exe` | Qt 6.11.1 | 2026-09-04 12:18:09 +08:00 | 失败，退出 `-1073740791` | Qt 帮助将 `--include-plugins` 定义为插件名；`platforms,sqldrivers` 是类型且与当前工作流不一致。失败后仅见 `qsqlite.dll`、未见 `qwindows.dll`，不得作为部署通过证据 |
| Qt 部署（当前工作流原命令） | `windeployqt --release --no-translations --plugindir build_verify_release\plugins --include-plugins qwindows,qsqlite build_verify_release\DeviceForge.exe` | Qt 6.11.1 | 2026-09-04 12:18:55 +08:00 | 通过，退出 0 | 与 `.github/workflows/release-validation.yml` 一致；`plugins\platforms\qwindows.dll` 1,006,904 字节，`plugins\sqldrivers\qsqlite.dll` 1,982,776 字节，均存在。工具另报找不到 `dxcompiler.dll`/`dxil.dll` 与未设置 `VCINSTALLDIR` 的非致命警告 |

## 4. 人工与外部环境验证

| 项目 | 命令或入口 | 环境 | 时间 | 结果 | 证据或阻塞 |
|---|---|---|---|---|---|
| GitHub Actions 首次远端运行 | GitHub Actions → `CI` 与 `Release Validation`；`workflow_dispatch` | GitHub-hosted Windows、Qt 6.9.2、Python 3.12 | 待定 | 待远端运行 | 分支尚未 push，无 Actions run URL；取得 URL、结论和 artifacts 后补录；本地 VS2026/Qt 6.11.1 结果不替代该项 |
| GUI Smoke | `D:\Qt\Tools\mingw1310_64\opt\bin\python3.exe tools/devtools/smoke.py build_verify_release\DeviceForge.exe`（只传 exe） | 正常 Windows 用户上下文；Python 3.9.10 | 2026-09-04 13:34:25 +08:00 | 未完成，环境阻塞，退出 1 | `SmokeRunner._default_find_window` 导入 `ctypes` 时发生 `ModuleNotFoundError: No module named '_ctypes'`（进程计时 0.273 秒）；未获得主窗口识别或 WM_CLOSE 证据，既不记产品窗口失败，也不记通过；未修改脚本或安装依赖 |
| NSIS 本地打包产物 | `Get-Command makensis`、`where.exe makensis`、`Test-Path 'C:\Program Files (x86)\NSIS\makensis.exe'` | 当前 Windows 主机 | 2026-09-04 13:35:12 +08:00 | 未执行打包，本机工具缺失 | PATH 未找到命令、约定路径不存在；待 `release-validation` 工作流安装 NSIS 后执行，不宣称本地打包通过 |
| 真实 FTP 部署 | FtpDeploy 工具 → FTP → G1-G5 人工验收 | 真实 FTP 设备与可交互桌面 | 待定 | 未执行 | 尚无设备清单、部署日志、报告文件及失败重试记录 |
| 真实 SFTP 部署 | FtpDeploy 工具 → SFTP → G1-G5 人工验收 | 真实 SFTP 设备与可交互桌面 | 待定 | 未执行 | 尚无设备清单、部署日志、报告文件及失败重试记录 |
| 本地 ftpd 3 台模拟 | 本地 ftpd ×3 → FtpDeploy 工具 | Windows 本地模拟环境 | 待定 | 未执行 | 尚无三台独立进度和结束态着色的屏幕/日志证据 |

## 5. 长稳验证

| 项目 | 命令或入口 | 环境 | 时间 | 结果 | 证据或阻塞 |
|---|---|---|---|---|---|
| 16 台模拟 50 轮 | 本地 ftpd/等价可控端点 ×16，连续执行 50 轮；记录每轮结果 | Windows Release；固定并发度与测试数据 | 待定 | 未执行 | 尚无轮次日志与成功/失败汇总 |
| 资源基线对比 | Task Manager/Process Explorer；记录开始、每 10 轮、结束时的内存、句柄、线程与连接数 | 与 16 台 50 轮同一进程和机器 | 待定 | 未执行 | 尚无基线与趋势数据，不能得出无泄漏结论 |

## 6. 当前结论与交接

v2.8 功能实现、合并提交和 Tag 均有 Git 证据；2026-09-04 在 VS2026 Community、MSVC 19.51、Qt 6.11.1 与 Ninja 下完成 Debug/Release 独立构建，两套正常用户上下文 CTest 均为 20/20，Python 为 25/25，九个版本来源均为 `2.8.0`。按当前 `release-validation` 工作流原命令执行的 Qt 部署退出 0，两个必需插件已落位。本地结果不替代远端 VS2022/Qt 6.9.2 工作流。

发布验收尚未闭环：GitHub Actions 首跑待执行；GUI Smoke 受本机指定 Python 缺少 `_ctypes` 阻塞；NSIS 本地工具缺失；真实 FTP/SFTP、本地 ftpd 3 台模拟和 16 台 50 轮长稳均未执行。执行者应把原始命令、环境、时间、结果及 URL/日志/artifact 路径回填到对应表格，不得仅凭实现提交或本地不同工具链结果改写为通过。
